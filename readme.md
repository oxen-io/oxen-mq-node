# oxen-mq napi bindings


to build for host system:

    $ npm install
    $ npm run clean
    $ npm run build:host
    
cross compile for windows:

    $ npm install
    $ npm run clean
    $ npm run build:win32

